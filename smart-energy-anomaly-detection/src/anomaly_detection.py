"""Anomaly-detection module scaffold.

Add the selected model only after the real dataset and evaluation protocol
are finalized.
"""

def model_status():
    return {
        "status": "scaffold",
        "note": "Add the validated anomaly-detection model here."
    }
